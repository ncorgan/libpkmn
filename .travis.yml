language: cpp

os:
 - linux

matrix:
  include:
    # Older GCC, older dependencies
    - compiler: gcc
      env:
        - DESIRED_QT_VERSION=4
        - PYTHON_VERSION=2
        - SWIG_VERSION=2
    # Older GCC, newer dependencies
    - compiler: gcc
      env:
        - DESIRED_QT_VERSION=5
        - PYTHON_VERSION=3
        - SWIG_VERSION=3
    # Newer GCC, older dependencies
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env:
        - NEWCC=gcc-6
        - NEWCXX=g++-6
        - DESIRED_QT_VERSION=4
        - PYTHON_VERSION=2
        - SWIG_VERSION=2
    # Newer GCC, newer dependencies
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env:
        - NEWCC=gcc-6
        - NEWCXX=g++-6
        - DESIRED_QT_VERSION=5
        - PYTHON_VERSION=3
        - SWIG_VERSION=3
    # Older Clang, older dependencies
    - compiler: clang
      env:
        - DESIRED_QT_VERSION=4
        - PYTHON_VERSION=2
        - SWIG_VERSION=2
    # Older Clang, newer dependencies
    - compiler: clang
      env:
        - DESIRED_QT_VERSION=5
        - PYTHON_VERSION=3
        - SWIG_VERSION=3
    # Newer Clang, older dependencies
    - compiler: clang
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-4.0
          packages:
            - clang-4.0
      env:
        - NEWCC=clang-4.0
        - NEWCXX=clang++-4.0
        - DESIRED_QT_VERSION=4
        - PYTHON_VERSION=2
        - SWIG_VERSION=2
    # Newer Clang, newer dependencies
    - compiler: clang
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-4.0
          packages:
            - clang-4.0
      env:
        - NEWCC=clang-4.0
        - NEWCXX=clang++-4.0
        - DESIRED_QT_VERSION=5
        - PYTHON_VERSION=3
        - SWIG_VERSION=3

sudo: required
dist: trusty

before_install:
  - git submodule update --init --recursive
  - bash testing/travis-scripts/install-deps.sh

script:
  - bash testing/travis-scripts/build-test.sh
